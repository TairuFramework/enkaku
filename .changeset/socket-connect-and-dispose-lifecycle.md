---
'@enkaku/socket': minor
'@enkaku/transport': patch
---

Socket connect and dispose lifecycle.

- `connectSocket(path, options?)` accepts `{ timeoutMs, signal }`, via the new exported `ConnectSocketOptions` type. The connect attempt now times out after **10 seconds by default** — pass `timeoutMs: 0` for the previous unbounded behaviour. An abandoned attempt (timeout or abort) destroys its pending socket, and both the `connect` and `error` listeners are detached once the promise settles (the `error` one previously stayed attached for the socket's whole life).
- The transport stream's writable close callback now awaits `socket.end()`'s flush, bounded by a 2 second grace, so `writer.close()` no longer resolves with bytes still queued. This makes `dispose()` itself slower in the case it used to paper over: the close callback used to be synchronous (`end(); unref()`); it now awaits the flush, up to 2 seconds.
- **`SocketTransport.dispose()` now destroys the socket** rather than only `unref()`ing it, and does so for every source shape — including a function source, which previously registered no release hook at all. `unref()` left the socket open, so a peer server could wait forever -- either for a transport that was never read from or written to (whose socket `end()` never ran), or for one whose peer still had data queued to an unreading client. A transport built from a function source that was never read from or written to opens no socket, and therefore has none to release. This means the transport now **takes ownership of a caller-supplied `Socket`** — `new SocketTransport({ socket })` will `destroy()` that socket on `dispose()`, even if the transport was never read from or written to.
- `SocketTransportParams` accepts `connectTimeoutMs`, applied when `socket` is a path string. Such a path is now connected **lazily**, on first read/write, rather than eagerly in the constructor.
- `CreateTransportStreamOptions<R>` gains `signal?: AbortSignal`. A `write()` stuck awaiting `'drain'` against a peer that never reads gets no `'drain'`, `'close'`, or `'error'` — nothing to settle it. `SocketTransport` now passes its own disposal signal through, so `dispose()` bounds that wait instead of hanging forever. On abort, the drain wait does **not** reject immediately: it gives the peer up to the same 2 second grace as the close flush above, so a peer that is merely stalled and resumes reading within that window still gets its write flushed in full, while a peer that never reads again still bounds `dispose()` at ~2 seconds and the socket ends up destroyed. An earlier version of this fix rejected the drain wait the instant the signal aborted, which bounded `dispose()` but reintroduced the truncation bug this changeset's flush-on-close fix (above) exists to prevent: the instant rejection errors the write, which errors the `WritableStream`, which makes `writer.close()` reject *without ever invoking its close callback* — skipping the flush-on-close grace entirely.
- A `write()` in flight when the transport is disposed now **rejects** with the `DisposeInterruption` the disposal signal carries, rather than silently never settling. Enkaku's own client and server route writes through `safeWrite`, which already treats this as a benign teardown (`writeDropped`, not `writeFailed`) — no change in behaviour there. A caller that floats a raw `transport.write()` with no `.catch()`, however, will now see an unhandled rejection where previously the promise just hung forever.
- `@enkaku/transport`: `Transport.dispose()` now emits `disposed` even when the underlying stream never resolved (a failed connect). Previously, awaiting the writer sat outside the `try`/`catch`, so that rejection escaped the whole dispose callback — `disposed` never fired, and a `console.warn` fired instead. Patch bump: pure bug fix, no public API change, but restores an invariant (`disposed` always fires) that consumers may already depend on to release resources.
